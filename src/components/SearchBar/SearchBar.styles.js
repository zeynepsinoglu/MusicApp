import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        padding: 8,
        margin: 10,
        borderRadius: 5,
        backgroundColor: "#eceff1",

    },
    searchTitle: {
        color: "black",
    }
})